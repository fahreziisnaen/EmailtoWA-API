services:
  app:
    build: .
    ports:
      - "3001:3001"
    container_name: email-forwarder
    environment:
      - IMAP_SERVER=172.16.0.1
      - IMAP_PORT=143
      - IMAP_USER=bitwarden
      - IMAP_PASSWORD=T3st@pi2o24